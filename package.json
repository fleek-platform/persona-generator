{
  "name": "persona-generator-monorepo",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "scripts/dev-watcher",
    "service:build": "sam build --build-in-source --parameter-overrides Environment=staging",
    "service:deploy": "bun run service:build && ./scripts/dev-deploy",
    "service:logs": "sam logs --stack-name persona-generator-service --name PersonaGeneratorService --tail"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
