#!/usr/bin/env bun

import { serve } from 'bun';

import { app } from './lambda/app';

const port = process.env.PORT || '3030';

serve({
  fetch: app.fetch,
  port,
});

process.stdout.write(`🐰 Preview API running at http://localhost:${port}`);
